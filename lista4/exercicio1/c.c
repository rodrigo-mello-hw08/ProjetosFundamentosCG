#include <stdio.h>

/*
Gerar e escrever os números inteiros do intervalo [25,70] em ordem decrescente
*/

int main() {
    for(int i=70 ; i >= 25 ; i--) {
        printf("\n%d", i);
    }

    return 0;
}